# --- Giao diện và Biểu đồ (Core UI & Visualization) ---
streamlit>=1.32.0
plotly>=5.19.0

# --- Xử lý dữ liệu (Data Manipulation) ---
pandas>=2.2.0
numpy>=1.26.0

# --- Đọc/Ghi dữ liệu (Data I/O) ---
# Bắt buộc phải có để pandas có thể đọc được file .parquet
pyarrow>=15.0.0 

# --- Phân tích & Machine Learning (Dự đoán rủi ro) ---
# Mở comment nếu trang 04_performance_risk.py của bạn có dùng các mô hình dự đoán
# scikit-learn>=1.4.0

# --- Môi trường phát triển (Development - Tùy chọn) ---
# Cần thiết để chạy các file .ipynb trong thư mục notebooks/
# jupyter>=1.0.0
# ipykernel>=6.29.0